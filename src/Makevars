LIBDIR = wrapper/target/release
STATLIB = $(LIBDIR)/wrapper.a
PKG_LIBS = -L$(LIBDIR) -lwrapper -lresolv

all: clean

$(SHLIB): $(STATLIB)

$(STATLIB):
	cargo build --release --manifest-path=wrapper/Cargo.toml

clean:
	rm -Rf $(SHLIB) $(STATLIB) $(OBJECTS) wrapper/target
